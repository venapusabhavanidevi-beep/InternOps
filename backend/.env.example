NODE_ENV=development
PORT=5000
SEED_ADMIN_EMAIL=admin@internops.com
SEED_ADMIN_PASSWORD=Admin@123
JWT_SECRET=your-jwt-secret-here
JWT_REFRESH_SECRET=your-refresh-secret-here
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
DATABASE_URL=postgresql://user:password@host/dbname
# Required only for backend tests. Must point to a separate disposable database.
TEST_DATABASE_URL=postgresql://test_user:test_password@test_host/test_db
# Deployed frontend origin (protocol + host, no trailing slash, e.g. https://internops.pages.dev).
# Comma-separated if multiple origins (e.g. production + preview deployments).
CORS_ORIGINS=http://localhost:5173
GROQ_API_KEY=your-groq-key
GEMINI_API_KEY=your-gemini-key
APP_URL=http://localhost:5173

# Redis is optional. REDIS_URL is recommended and is shared by the backend
# cache/session features and BullMQ. Use rediss:// for TLS providers.
REDIS_URL=redis://localhost:6379/0

# Alternative host-based configuration (used only when REDIS_URL is empty)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_USERNAME=default
# REDIS_PASSWORD=
# REDIS_DB=0
# REDIS_TLS=false

# Legacy Upstash alternative (used only when REDIS_URL and REDIS_HOST are empty)
# UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-redis-token
# File Storage (Optional Cloudinary / Cloudflare R2 / AWS S3 integration. Defaults to 'local' uploads/ directory if unconfigured)
STORAGE_DRIVER=cloudinary              # 'cloudinary', 'r2', 's3', or 'local'
CLOUDINARY_CLOUD_NAME=mugdha-naik
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# R2 / S3 Alternative Options
# R2_ACCOUNT_ID=your-cloudflare-account-id
# R2_ACCESS_KEY_ID=your-r2-access-key
# R2_SECRET_ACCESS_KEY=your-r2-secret-key
# R2_BUCKET_NAME=internops-uploads
# R2_PUBLIC_URL=https://pub-your-bucket.r2.dev

METRICS_TOKEN=your-metrics-token-here

# ERROR TRACKING (optional, set DSN to enable)
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1

# API versioning — activate when v1 enters its sunset window (≥ 90 days notice)
# V1_DEPRECATED=true
# V1_DEPRECATION_DATE=Sat, 01 Jan 2028 00:00:00 GMT
# V1_SUNSET_DATE=Mon, 01 Apr 2028 00:00:00 GMT

# ============================================
# Email (SMTP) – REQUIRED for sending lockout notifications
# ============================================
SMTP_HOST=smtp.gmail.com               # e.g., smtp.gmail.com, smtp.office365.com
SMTP_PORT=587                          # 587 for TLS (STARTTLS), 465 for SSL
SMTP_SECURE=false                      # true for port 465, false for 587
SMTP_USER=your-email@gmail.com         # sender email address
SMTP_PASS=your-app-password            # use app password for Gmail
EMAIL_FROM=noreply@internops.com       # "From" address (can be same as SMTP_USER)

# Request throttling. Development defaults allow repeated UI refresh testing
# while route-specific session bootstrap limits remain bounded per IP.
RATE_LIMIT_GLOBAL_MAX=1000
RATE_LIMIT_AUTH_MAX=200
RATE_LIMIT_REFRESH_MAX=60
RATE_LIMIT_CSRF_MAX=300
RATE_LIMIT_TIME_WINDOW=1 minute
