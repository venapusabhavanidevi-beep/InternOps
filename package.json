{
  "name": "internops",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "description": "InternOps is an enterprise-grade workforce management platform designed to streamline intern operations, attendance tracking, and performance monitoring within structured team hierarchies.",
  "main": "load-processor.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "npm run test --workspace=backend",
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\" \"cd ai-service && uv run uvicorn app.main:app --reload\"",
    "dev:app": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "lint": "npm run lint --workspaces --if-present",
    "format": "prettier --write .",
    "clean": "rimraf node_modules && npm run clean --workspaces --if-present",
    "reinstall": "npm run clean && npm install",
    "prepare": "husky || exit 0",
    "prompt-test": "pytest ai-service/app/tests"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rajat-wyrm/InternOps.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/rajat-wyrm/InternOps/issues"
  },
  "homepage": "https://github.com/rajat-wyrm/InternOps#readme",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "concurrently": "^10.0.3",
    "eslint": "^10.7.0",
    "globals": "^17.7.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.9.5",
    "rimraf": "^6.1.3"
  },
  "lint-staged": {
    "*": ".\\node_modules\\.bin\\prettier.cmd --write --ignore-unknown"
  },
  "dependencies": {
    "@google/genai": "^2.16.0",
    "@sentry/react": "^10.73.0",
    "@sentry/conventions": "^0.16.0",
    "bullmq": "^6.1.1",
    "color-name": "^2.1.1",
    "require-directory": "^2.1.1",
    "sonner": "^2.0.7"
  }
}
